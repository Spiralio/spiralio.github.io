// Template Style

styles.temp = {
    name: 'Template'
}

/**
 * Runs when loaded
 */
styles.temp.load = () => {
    return;
}

/**
 * Runs when unloaded
 */
styles.temp.unload = () => {
    return;
}

/**
 * Runs every frame
 */
styles.temp.frame = () => {
    return;
}